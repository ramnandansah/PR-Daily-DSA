// Program to Calculate the Area of a Triangle:--

const areaOfTriangle = (base, height) => {
    return `The area of the triangle is ${base * height / 2}.`;  // area of trianle is = (base * height) / 2
}

console.log(areaOfTriangle(2, 4)); // output: The area of the triangle is 4.